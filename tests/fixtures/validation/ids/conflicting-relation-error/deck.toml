# - two `follows` relations where the specification allows one
# - the `surrogate_for` target is also named under `follows`
[deck]
schema_version = "2.0"
name = "Conflicting Relations"
version = "1.0"
identifier = "org.example/deck/conflicting-relations"
related = [
  { rel = "follows", deck = "land.arcana/deck/rider-waite-smith" },
  { rel = "follows", deck = "com.example/deck/other" },
  { rel = "surrogate_for", deck = "com.example/deck/other" },
]
