# - path is `tarot/<name>` rather than `deck/<name>`
# - the deck stands in for itself
[deck]
schema_version = "2.0"
name = "Odd Shape"
version = "1.0"
identifier = "org.example/tarot/odd-shape"
related = [
  { rel = "surrogate_for", deck = "org.example/tarot/odd-shape" },
]
