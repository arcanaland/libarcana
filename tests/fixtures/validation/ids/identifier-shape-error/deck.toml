# - path is `tarot/<name>` rather than `deck/<name>`
# - the deck signifies itself
[deck]
schema_version = "2.0"
name = "Odd Shape"
version = "1.0"
identifier = "org.example/tarot/odd-shape"
signifies = "org.example/tarot/odd-shape"
