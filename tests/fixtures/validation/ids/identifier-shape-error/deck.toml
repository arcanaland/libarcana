# The path is `tarot/<name>` rather than `deck/<name>`, and the deck signifies
# itself. Both values are well-formed qualified identifiers.
[deck]
schema_version = "2.0"
name = "Odd Shape"
version = "1.0"
identifier = "org.example/tarot/odd-shape"
signifies = "org.example/tarot/odd-shape"
