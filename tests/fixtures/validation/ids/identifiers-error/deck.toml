# - A bare label is not a realm
# - `also_bad` has no slash
# - [app] subtable key is unquoted
[deck]
schema_version = "2.0"
name = "Bad Identifiers"
version = "1.0"
identifier = "notarealm/deck/bad-identifiers"
signifies = "also_bad"

[app.land.arcana]
bleed = true
